# MongoDB Connection String
# Get this from MongoDB Atlas: https://cloud.mongodb.com/
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database

# JWT Secret Key
# Generate a secure random string for production
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_PASSWORD=your-secure-jwt-secret-key-here

# Server Port
PORT=3000

# Frontend URLs (comma-separated)
# Add all your frontend URLs that should be allowed to access the API
FRONTEND_URLS=http://localhost:5173,https://your-frontend.vercel.app

# Node Environment
# Set to "production" in deployment
NODE_ENV=development